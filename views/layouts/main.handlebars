<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
  <link href='https://css.gg/add-r.css' rel='stylesheet'>
  <link href='https://css.gg/shopping-cart.css' rel='stylesheet'>
  <link rel="stylesheet" href="/stylesheets/style.css">
  <title>Contactless Menu</title>
</head>

<body class="d-flex h-100 text-center text-white bg-dark">

  <div class="cover-container d-flex w-100 h-100 p-3 mx-auto flex-column">
    <header class="mb-auto">
      <div class='d-flex justify-content-between align-items-center'>
        <a href='/'>
          <h3 class="float-md-start mb-0">Contactless Menu</h3>
        </a>
        
        <nav class="nav nav-masthead d-flex justify-content-end align-items-center float-md-end">
          <p id='cart-alert'></p>
          <a id='cart-btn'>
            <i class="gg-shopping-cart"></i>
          </a>
        </nav>
      </div>
    </header>

    <main class="px-3">
      {{{body}}}
    </main>

    <footer class="text-white-50">
      <div class='d-flex flex-column justify-content-between align-items-center'>
        <p>Thanks for visiting</p>

        {{#if loggedIn}}
        <a id="logout" class="nav-link" href="/logout">logout</a>
        {{else}}
        <a class="nav-link" href="/login">Employee login</a>
        {{/if}}
      </div>
    </footer>
  </div>

  <script src='/javascript/main.js'></script>
  {{#if loggedIn}}
  <script src="/javascript/logout.js"></script>
  {{/if}}
</body>

</html>